# ==============================================================================
# .env.example — Plantilla de variables de entorno
#
# INSTRUCCIONES:
#   1. Copia este archivo como .env en la raiz del proyecto.
#   2. Completa los valores marcados con <...>.
#   3. NUNCA subas el archivo .env al repositorio (ya esta en .gitignore).
# ==============================================================================

# Clave de OpenAI — REQUERIDA
# Obtener en: https://platform.openai.com/api-keys
OPENAI_API_KEY=<tu-clave-aqui>

# Configuracion opcional del modelo (estos son los valores por defecto)
# MODELO=gpt-4o-mini          # modelo a usar
# TEMPERATURA=0.2             # temperatura del LLM (0.0 = determinista, 1.0 = creativo)
